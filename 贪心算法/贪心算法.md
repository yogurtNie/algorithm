# 贪心算法
- 概念：在每一次都要求局部最优，而不要求全局最优
- 贪心其实不要求知其所以然
  - 但是如何验证？对数器的方式来进行验证，然后通过随机产生器
- **注意**
  - 不要纠结严格证明，浪费时间且收益很低
  - 解法只包含贪心思路的题目，代码量不大
  - 重点是贪心方式的特征也就是题目的特征，而不是证明，累积经验很重要
  - 用对数器来验证正确性就好
  - 如果数据量能够支持非贪心策略，则优先去贪心
  - 广义的贪心无处不在，可能和别的思路结合，可以通过自然智慧想明白，不用纠结证明
# 例题
## 题目一：最大数
- 由经典问题改动：将一个数组中所有的字符片段都用上，拼成一个字典序最小的字符串
- 有a和b两个字符片段，有两种排序策略：
  - 若a>b，则a在b的后面；反之，a在b的前面
  - 若a.b（代表a拼接上b）> b.a，则采用b.a，反之，采用a.b
  - 上述两种策略中，前一种策略是错误的，后一种策略是正确的
- 证明第二种策略是正确的
  - 首先，该策略具有传递性，即a.b<b.a, b.c<c.b，则有a.c<c.a
    - 定义f(k) = 对应进制^k的位数，比如k = "abc"，则f(k) = 26^3，因为字母为26进制，且abc长度为3
    - a拼接上b = a * f(b) + b
    - 上述的两个条件变成
      - a * f(b) + b <= b * f(a) + a; b * f(c) + c <= c * f(b) + b
      - 经过变换可以得到最终的式子f(c) * a + c <= f(a) * c + a
  - 按照上述策略连接得到的字符串，经过任何一次交换都一定变得更大(传递性)，因为就必定是最小的字符串
- **提醒**：真正的贪心证明实在是很繁琐，所以最好掌握对数器验证

## 题目二：两地调度
- 策略是，先让所有人都去a地，然后计算从a切换到b要付出的代价，这个代价可能是正也可能是负，然后从小到大取需要的人数转去b
- 比如，0号为[5,10]，1号为[12,20]，都去a为17,0切换需要+5,1切换需要+8，则让0切换

## 题目三：吃橘子的最小天数
- 当n大于1的时候，必定要按比例吃是最快的
- 如果无法按比例吃，每天吃1个橘子是为了能够之后按比例吃
  - 比如，如果当前的橘子数量无法被2整除，就需要付出1天的代价，然后按2的比例来吃
  - 如果当前橘子的数量n满足，$n % 3 = 1$，则付出1天的代价，如果满足$n%3=2$，则付出2天的代价
- 优化策略：记忆化递归，dp(i,j)代表i个橘子最少需要j天吃完，用一个map来记录

## 题目四：会议室II
- 问需要最少会议室，其实就是问一个时间段内最多重合的线段数量，也就是最多线段重合问题
- 用最小堆来解决，本质上是在求，在当前线段开始的时候，还有多少线段还没结束，然后线性遍历一遍，取最大值就好

## 题目五：课程表III
- 问最多能修多少门课程，策略是排序 + 最大堆回退
- 将数组按照结束时间来排序，按照越早结束越好来遍历；每次考虑是否纳入答案的时候有两条原则
  - 如果加入这个代价没有超过限制，则无条件纳入
  - 如果超过限制，则考虑上一个已经加入答案集的代价最大的选择能否被替换，能被替换则必定替换

## 题目六：合并果子
- 哈夫曼树编码的模型。
- 另一逆过程问题：如果有10米长的金条，有一个数组，要求将该金条分成数组对应的数字的最优策略（最小代价）
  - 本质上也是哈夫曼树的编码
  - 用最小堆解决

## 题目七：砍竹子II
- 在刚拿到题目的时候可能会认为需要用动态规划来做，但是不行，原因是乘积得到的中间结果必然很大，所以势必要取模
- 但是取模会**丧失数字之间的大小关系**，所以无法判断最大乘积是谁
- 纯观察，我们可以在以下的式子中发现某些规律

n = 4  --> 2 * 2         |  n = 5  --> 3 * 2         |  n = 6  --> 3 * 3
n = 7  --> 3 * 2 * 2     |  n = 8  --> 3 * 3 * 2     |  n = 9  --> 3 * 3 * 3
n = 10 --> 3 * 3 * 2 * 2 |  n = 11 --> 3 * 3 * 3 * 2 |  n = 12 --> 3 * 3 * 3 * 3
**规律**：n % 3 == 0，则为$3 ^ ((n - 4) / 3) * 4$，n % 3 == 1，则为$3 ^((n-2)/3)*2$，n % 3 == 2，则为$3^(n/3)$

- 为加速运算，此处用快速幂来计算power3

## 题目八：n的k份的最大乘积
- 最靠近平均值的乘积就是最大乘积
- 用对数器来验证
  - 暴力解：直接用暴力递归来进行计算，每次1,1地这样分出去，分的规则上限是
    - 只剩k=1时返回
    - 分出去的i必须比可分的数字小，剩下的数字必须比剩下的要分份数大或者相等

## 题目九：最大会议数量
- 我的初步思路：利用最小堆和排序计数
- 按照结束时间早的会议来安排
- 用对数器来验证
  - 暴力解：全排列暴力

## 题目十：最大会议数量II
- 现在的思路是，利用小根堆和排序计数
- 排序按照开始时间来排序，然后按照开始的天数来考虑，如果有多个都会开始，那么用小根堆选最早结束的来

## 题目十一：IPO
- 利用最大堆和排序计数
- 排序按照资本大小来排，然后选能选的任务中收益最大的来做
- 小提醒：不要用vector<vector<int>>来嵌套，会很慢，用struct

## 题目十二：计算数组的任意两个数的差值的绝对值
- 裴蜀定理：最大公约数可以表示成两个数的线性组合
  - 形式化地说，任意整数a，b，存在整数x，y使得ax + by = gcd(a,b)存在
  - 本质上，求b-a或者a-b，然后反复用差值减其中的某个数，就是在不断构造xa + yb的过程，也就是线性组合的过程
  - 则生成的数都应该是gcd(a,b)的倍数
- 所以我们滚动计算所有数组的元素的最大公约数，小于最大值的所有最大公约数的倍数都会最终被加入到数组中
- 要处理两个边界问题
  - 第一是零的讨论，因为在gcd的计算中我们跳过了0。如果数组中曾经有0，那么就直接把0的数量给加上就好；如果没有出现0，那么只要出现过重复数组，就要再+1
  - 第二是重复数字的讨论，因为gcd一定会把原来数组中的元素生成时都包含一遍，所以要统计不止出现一次的数字，将数字的出现次数-1，再加回答案上

## 题目十三：最短无序连续子数组
- 思考怎么确定两端的边界，什么时候可以节省下前后两段，就是波动式上升
  - 要确定左边界，那就是左边可节省范围的最大值不能大于子数组的任何一个数，否则就肯定要包含这个最大值
  - 要确定右边界，那就是右边可节省范围的最小值不能小于子数组的任何一个数，否则就肯定包含这个最小值

## 题目十四：k个长度的最短覆盖区间
- 利用有序表的性质，每次都在k个list中依次取出数，加入有序表，并计算开头和结尾的距离
- 不断继续，直到把某个列表遍历完，无法继续更新和确保包含所有数
- 借助结构体和set，来同时确保所有数组的值都被包含，且这个范围是最小的

## 题目十五：买门票的最大价格
- 传统视角下，我们会以整体人数为角度来衡量门票钱，比如
  - 1个人来玩，那么1个人交 10 - 2*1 = 8
  - 2个人来玩，那么2个人交 2 * (10 - 2 * 2) = 12
  - 3个人来玩，那么3个人交 3 * (10 - 2 * 3) = 12
  - 4个人，交 4 * 2 = 8
  - 5个人，交0元
  - 但这种决策和衡量的角度并不利于在线决策，换句话说不利于贪心，因为每次状态都和整个人数有关，牵一发而懂全省
- 所以我们更换视角，以单个人数带来的利润增量为衡量指标，比如
  - 1个人来，这个人交8元
  - 2个人来，新来的人交4元，总共12元
  - 3个人来，新来的人交0元，总共12元
  - 4个人来，新来的人交-4元，总共8元
  - 5个人来，新来的人交-8元，总共0元
  - 这个时候的思维惯性可能会去到差分数组，但是差分数组的坏处在于，有m个项目，每个项目都要去跑n遍，m*n的复杂度无法接受
- 那么则借助大根堆，和公式来计算当前人到达的时候需要交多少钱
  - 设当前人数为x，第x+1到来的时候，需要额外支付的费用为 $Bi - (x+1)*Ki - x*Ki$

